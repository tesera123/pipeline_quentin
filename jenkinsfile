pipeline {
    agent {
        docker {
            label 'docker_node'
        }
    stages {

        stage('init') {
            steps {
                sh 'docker run hello-world'
            }
        }
    }
}
